# 인벤토리 → 전투 스탯 연동 아키텍처 문서

> **작성일**: 2025-11-16  
> **프로젝트**: 텍스트 RPG (Dart/Flutter)

---

## 📊 전체 구조 개요

```
┌────────────────────────────────────────────────────────────────┐
│                     Player (RPG 캐릭터)                         │
│  strength, agility, intelligence, charisma                     │
│  → 전투에 영향 없음! 선택지 확률/인카운터 조건에만 사용         │
│  vitality → 전투 시작 HP 계산에만 사용 (vitality * 25)         │
└────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌────────────────────────────────────────────────────────────────┐
│              InventorySystem (그리드 인벤토리)                  │
│                                                                 │
│  ┌──────────────────┐         ┌──────────────────┐            │
│  │  placedItems     │         │  unplacedItems   │            │
│  │  (배치됨 = 장착) │         │  (미배치 = 보관) │            │
│  │  ✓ 스탯 반영     │         │  ✗ 스탯 무시     │            │
│  └──────────────────┘         └──────────────────┘            │
│                                                                 │
│  각 InventoryItem.properties:                                  │
│  {                                                              │
│    'combat': {                                                  │
│      'attackPower': 10,                                         │
│      'maxHealth': 20,                                           │
│      'accuracy': 5,                                             │
│      'defenseRate': 0.1  // 10% 방어                           │
│    },                                                           │
│    'weapon': {                                                  │
│      'baseDamage': 15.0,                                        │
│      'staminaCost': 5.0,                                        │
│      'cooldown': 1.0,                                           │
│      'accuracy': 0.75,                                          │
│      'type': 'melee'                                            │
│    }                                                            │
│  }                                                              │
└────────────────────────────────────────────────────────────────┘
                                  │
                  전투 시작 시 (EnterCombat 이벤트)
                                  │
                                  ▼
┌────────────────────────────────────────────────────────────────┐
│              InventoryAdapter.calculateTotalStats()            │
│                                                                 │
│  1. placedItems를 순회                                         │
│  2. 각 item.properties['combat'] 추출                          │
│  3. 시너지 효과 계산 (SynergySystem)                           │
│  4. 모든 스탯 합산                                              │
└────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
                        CombatStats (보너스)
                    attackPower: +25, maxHealth: +50
                                  │
                                  ▼
┌────────────────────────────────────────────────────────────────┐
│       baseStats (Player.vitality 기반)                         │
│       + inventoryBonus (인벤토리 아이템)                        │
│       = finalStats                                              │
└────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌────────────────────────────────────────────────────────────────┐
│              Character (전투 캐릭터)                            │
│  stats: finalStats                                              │
│  weapons: [추출된 무기들]                                       │
│  inventorySystem: (복사본, UI 표시용)                          │
└────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
                             전투 진행
                    (CombatEngine, 100ms 틱)
```

---

## 🔑 핵심 개념

### 1. "그리드 배치 = 장착"

전통적인 RPG의 "장비 슬롯" 개념이 아닌, **Resident Evil 4 스타일**:

- **배치된 아이템 (`placedItems`)**: 스탯에 영향 ✅
- **미배치 아이템 (`unplacedItems`)**: 스탯에 영향 없음 ❌

### 2. RPG 스탯 vs 전투 스탯

| 스탯 | 용도 | 전투 영향 |
|------|------|-----------|
| `Player.strength` | 선택지 확률, 인카운터 조건 | ❌ 없음 |
| `Player.agility` | 선택지 확률, 인카운터 조건 | ❌ 없음 |
| `Player.intelligence` | 선택지 확률, 인카운터 조건 | ❌ 없음 |
| `Player.charisma` | 선택지 확률, 인카운터 조건 | ❌ 없음 |
| `Player.vitality` | 전투 시작 HP = vitality * 25 | ⚠️ 시작 시만 |
| `Player.sanity` | 게임 오버 조건 | ❌ 없음 |
| `InventoryItem.properties['combat']` | 전투 스탯 직접 제공 | ✅ 전부 |

### 3. 전투 중 인벤토리 잠금

```dart
// 전투 시작 시
inventory.lockSystem.lock(reason: InventoryLockReason.combat);

// 전투 중 모든 조작 차단:
// - 아이템 이동 ❌
// - 아이템 회전 ❌
// - 아이템 추가/제거 ❌
// - 드래그 시작 ❌

// 전투 종료 시
inventory.lockSystem.unlock();
```

**결론**: 전투 중 인벤토리 변경 불가 → **동적 스탯 갱신 불필요**

---

## 📁 주요 파일 및 역할

### 핵심 변환 로직

#### `lib/modules/combat/inventory_adapter.dart`
- **역할**: 인벤토리 → 전투 스탯 변환의 모든 로직
- **주요 메서드**:
  ```dart
  // 1. 단일 아이템 → 스탯
  static CombatStats extractCombatStats(InventoryItem item)
  
  // 2. 시너지 → 스탯
  static CombatStats extractSynergyStats(SynergyInfo synergy)
  
  // 3. 인벤토리 전체 → 합산 스탯
  static CombatStats calculateTotalStats(InventorySystem inventory)
  
  // 4. 플레이어 캐릭터 생성 (스탯 적용 + 무기 추출)
  static Character createPlayerCharacter({
    required String name,
    required CombatStats baseStats,
    required InventorySystem inventory,
  })
  
  // 5. 무기 추출 (properties['weapon'] → Weapon 객체)
  static List<Weapon> extractWeapons(InventorySystem inventory)
  ```

### 전투 시작

#### `lib/modules/combat/combat_module.dart`
- **역할**: 전투 시작 시 Player + Inventory → Character 생성
- **핵심 코드**:
  ```dart
  // 1. Player.vitality 기반 baseStats
  final playerBaseStats = CombatStats(
    maxHealth: (vm.player?.vitality ?? 4) * 25,
    currentHealth: (vm.player?.vitality ?? 4) * 25,
    attackPower: 0,  // 기본 공격력 0, 아이템에서만
    accuracy: 75,
  );
  
  // 2. 인벤토리 적용
  final playerChar = InventoryAdapter.createPlayerCharacter(
    name: '모험가',
    baseStats: playerBaseStats,
    inventory: vm.playerInventory!,
  );
  
  // 3. 인벤토리 잠금
  vm.playerInventory!.lockSystem.lock(
    reason: InventoryLockReason.combat,
  );
  ```

### 인벤토리 시스템

#### `lib/inventory/inventory_system.dart`
- **역할**: 그리드 기반 인벤토리 관리
- **주요 속성**:
  - `placedItems`: 배치된 아이템 (스탯 반영)
  - `unplacedItems`: 미배치 아이템 (스탯 무시)
  - `lockSystem`: 전투 중 잠금 시스템

#### `lib/inventory/combat_lock_system.dart`
- **역할**: 전투 중 인벤토리 조작 차단
- **주요 메서드**:
  ```dart
  void lock({InventoryLockReason reason, String? info})
  void unlock()
  LockCheckResult canPerformAction(String actionName)
  ```

### 데이터 모델

#### `lib/core/character/character_models.dart`
- **Player**: RPG 캐릭터 (전투 외 스탯)
- **Trait**: 특성 시스템

#### `lib/combat/character.dart`
- **Character**: 전투용 캐릭터
- **CombatEngine**: 전투 루프 (100ms 틱)

#### `lib/combat/stats.dart`
- **CombatStats**: 전투 스탯 (attackPower, maxHealth, accuracy, defenseRate)

---

## 💾 저장/로드 시스템

### 인벤토리 직렬화

#### `lib/inventory/inventory_serialization.dart`
```dart
// 저장
final json = InventorySerialization.inventoryToJson(inventory);

// 로드
InventorySerialization.inventoryFromJson(json, inventory);
```

**저장 내용**:
- ✅ 그리드 크기
- ✅ 모든 아이템 ID, 이름, 설명
- ✅ 아이템 위치 (x, y)
- ✅ 회전 상태 (isRotated)
- ✅ **properties (combat, weapon 데이터 포함)**

**주의**: 
- `properties['combat']`에 스탯이 저장되므로 세이브 데이터에 포함됨
- 시너지는 로드 후 자동 재계산됨

### SaveData 구조

#### `lib/save_system.dart`
```dart
class SaveData {
  final DateTime timestamp;
  final String currentScene;
  final Map<String, int> stats;  // Player RPG 스탯
  final List<String> items;      // 간단한 아이템 ID 리스트 (레거시)
  final Map<String, dynamic>? inventory;  // 인벤토리 전체 (JSON)
  // ...
}
```

**개선 사항**:
- ✅ `inventory` 필드에 `InventorySerialization.inventoryToJson()` 결과 저장
- ✅ properties를 통해 아이템별 스탯도 저장됨
- ⚠️ `items` 필드는 레거시 (이제는 `inventory`가 메인)

---

## 🎯 장점과 한계

### ✅ 장점

1. **명확한 단일 소스**: 
   - 전투 스탯 = 인벤토리 배치 상태
   - 중복 저장/수동 동기화 불필요

2. **유연한 장착 시스템**:
   - 슬롯 제한 없음 (배치 가능한 만큼 장착)
   - 시너지로 전략적 배치 유도

3. **잠금 메커니즘**:
   - 전투 중 인벤토리 조작 불가
   - 밸런스 유지 + 성능 최적화

4. **데이터 기반**:
   - 아이템 정의는 properties에 저장
   - 하드코딩 없음

### ⚠️ 한계

1. **전통적 RPG와 다름**:
   - "무기/방어구/악세서리 슬롯" 개념 없음
   - 무기 10개 모두 배치 = 10개 모두 스탯 합산
   - → 게임 디자인에 따라 장점/단점

2. **Player 스탯 단절**:
   - Player.strength가 전투 공격력에 영향 없음
   - → 의도된 설계 (선택지 시스템과 분리)

3. **전투 시작 시점 스냅샷**:
   - 전투 중 레벨업/장비 변경 불가
   - → 전투 종료 후 반영

---

## 🔧 개선 제안 (선택 사항)

### 1. 장비 슬롯 제한 (필요한 경우)

현재는 배치 가능한 모든 아이템이 스탯에 영향을 줍니다. 
슬롯 제한을 원한다면:

```dart
// 예시: InventoryItem에 equipmentSlot 추가
class InventoryItem {
  final String? equipmentSlot;  // 'mainWeapon', 'armor', 'accessory', null
  // ...
}

// InventoryAdapter 수정
static CombatStats calculateTotalStats(InventorySystem inventory) {
  // 슬롯당 1개만 계산
  final slotMap = <String, InventoryItem>{};
  
  for (final item in inventory.placedItems) {
    final slot = item.equipmentSlot;
    if (slot != null) {
      slotMap[slot] = item;  // 중복 시 덮어쓰기
    }
  }
  
  // slotMap의 아이템만 스탯 계산
  // ...
}
```

### 2. Player 스탯 → 전투 보너스 (필요한 경우)

현재는 Player.strength가 전투에 영향 없지만, 소폭 보너스를 주려면:

```dart
// CombatModule에서
final playerBaseStats = CombatStats(
  maxHealth: (vm.player?.vitality ?? 4) * 25,
  currentHealth: (vm.player?.vitality ?? 4) * 25,
  attackPower: (vm.player?.strength ?? 3) * 2,  // 힘 1당 +2 공격력
  accuracy: 75 + (vm.player?.agility ?? 3) * 2,  // 민첩 1당 +2 명중
);
```

**주의**: 현재는 의도적으로 분리되어 있으므로, 변경 시 게임 디자인 재검토 필요

### 3. 전투 중 인벤토리 조작 허용 (권장하지 않음)

현재는 전투 중 인벤토리가 잠기지만, 허용하려면:

```dart
// InventorySystem에 리스너 추가
inventory.onItemMoved.listen((item) {
  if (combat.isActive) {
    // 스탯 재계산
    final newBonus = InventoryAdapter.calculateTotalStats(inventory);
    character.updateStats(baseStats + newBonus);
  }
});
```

**단점**: 
- 성능 문제 (실시간 재계산)
- 밸런스 문제 (전투 중 무제한 장비 교체)
- 잠금 시스템 무력화

---

## 📝 TODO (필요한 경우)

### 현재 구조 유지 시

- ✅ 구조 문서화 완료
- ✅ 주석 추가 완료
- ✅ 세이브/로드 검증 완료
- ✅ 잠금 시스템 검증 완료

### 향후 개선 사항 (선택)

- [ ] 장비 슬롯 제한 추가 (게임 디자인 요구 시)
- [ ] Player 스탯 → 전투 보너스 추가 (밸런스 조정 시)
- [ ] 아이템 타입별 최대 개수 제한 (예: 무기 최대 3개)
- [ ] UI에서 "장착된 아이템" 표시 개선
- [ ] 전투 전 인벤토리 프리뷰 기능

---

## 🎓 결론

현재 시스템은:
- ✅ **완전히 구현되어 작동 중**
- ✅ **단일 소스 원칙 준수** (인벤토리가 전투 스탯의 유일한 소스)
- ✅ **데이터 기반** (하드코딩 없음)
- ✅ **세이브/로드 호환**
- ✅ **전투 중 불변성 보장** (잠금 시스템)

추가 작업이 필요하다면:
1. 게임 디자인 방향 결정 (슬롯 제한 여부 등)
2. 이 문서의 "개선 제안" 섹션 참고
3. 코드 수정 후 기존 세이브 데이터 호환성 테스트

---

**문서 작성자**: AI Assistant  
**검토 필요**: 게임 디자이너, 시스템 프로그래머




