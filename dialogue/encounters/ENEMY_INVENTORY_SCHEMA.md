# 적 인벤토리 스키마 가이드

## 개요

인카운터 메타데이터에 `enemyInventory` 섹션을 추가하여 적의 장비를 정의할 수 있습니다.
**Manual, Auto, Hybrid** 세 가지 모드를 지원합니다.

---

## 📋 기본 구조

```json
{
  "metadata": {
    "combat": {
      "enemyName": "적 이름",
      "enemyType": "bandit | monster | undead | mage",
      "enemyStats": {
        "maxHealth": 100,
        "attackPower": 20,
        "accuracy": 75
      },
      "enemyInventory": {
        "mode": "manual | auto | hybrid",
        // 모드별 추가 필드
      }
    }
  }
}
```

---

## 🎯 Mode 1: Manual (수동 배치)

완전히 수동으로 아이템을 배치합니다. 보스전이나 스토리 중요 전투에 적합합니다.

```json
{
  "enemyInventory": {
    "mode": "manual",
    "grid": {
      "width": 9,
      "height": 6
    },
    "items": [
      {
        "id": "rusty_dagger",
        "position": {"x": 0, "y": 0},
        "rotation": 0,
        "comment": "설명 (선택사항)"
      },
      {
        "id": "leather_armor",
        "position": {"x": 2, "y": 0},
        "rotation": 0
      },
      {
        "id": "healing_potion",
        "position": {"x": 5, "y": 1},
        "rotation": 0
      }
    ]
  }
}
```

### 필드 설명
- `mode`: `"manual"` (필수)
- `grid.width`: 인벤토리 가로 크기 (기본값: 9)
- `grid.height`: 인벤토리 세로 크기 (기본값: 6)
- `items`: 아이템 배열
  - `id`: 아이템 ID (필수)
  - `position.x`: X 좌표 (0부터 시작)
  - `position.y`: Y 좌표 (0부터 시작)
  - `rotation`: 회전 (0~3, 기본값: 0)
  - `comment`: 메모 (선택)

### 장점
✅ 정확한 밸런스 제어  
✅ 스토리 테마에 맞는 아이템  
✅ 위치 기반 시너지 가능

### 단점
❌ 설정 작업량 많음  
❌ 리플레이 시 항상 동일

---

## ⚙️ Mode 2: Auto (자동 생성)

난이도와 레벨에 따라 자동으로 아이템을 생성합니다. 랜덤 인카운터에 적합합니다.

```json
{
  "enemyInventory": {
    "mode": "auto",
    "autoGeneration": {
      "difficulty": "easy | normal | hard | boss",
      "level": 5,
      "enemyType": "bandit",
      "itemBudget": 200,
      "guaranteed": ["healing_potion"],
      "excludeTypes": ["consumable"]
    }
  }
}
```

### 필드 설명
- `mode`: `"auto"` (필수)
- `autoGeneration.difficulty`: 난이도 (필수)
  - `"easy"`: 기본 예산 50
  - `"normal"`: 기본 예산 100
  - `"hard"`: 기본 예산 200
  - `"boss"`: 기본 예산 400
- `autoGeneration.level`: 적 레벨 1~10 (레벨당 +20% 예산)
- `autoGeneration.enemyType`: 적 타입 (아이템 풀 필터링용, 선택)
- `autoGeneration.itemBudget`: 직접 예산 지정 (선택, 기본값은 난이도+레벨로 계산)
- `autoGeneration.guaranteed`: 반드시 포함할 아이템 ID 배열 (선택)
- `autoGeneration.excludeTypes`: 제외할 아이템 타입 (선택)

### 예산 계산식
```
최종 예산 = 기본예산(난이도) × (1.0 + (레벨 - 1) × 0.2)

예시:
- normal, level 1 → 100
- normal, level 5 → 100 × 1.8 = 180
- hard, level 10 → 200 × 2.8 = 560
- boss, level 8 → 400 × 2.4 = 960
```

### 장점
✅ 빠른 설정  
✅ 리플레이 다양성  
✅ 자동 밸런싱

### 단점
❌ 예측 불가능  
❌ 테마 부조화 가능

---

## 🎖️ Mode 3: Hybrid (하이브리드)

필수 아이템은 수동 배치 + 나머지는 자동 생성. 보스전 등 중요하면서도 다양성이 필요한 경우 적합합니다.

```json
{
  "enemyInventory": {
    "mode": "hybrid",
    "items": [
      {
        "id": "legendary_weapon",
        "position": {"x": 0, "y": 0},
        "rotation": 0,
        "comment": "보스 전용 무기 (필수)"
      },
      {
        "id": "dragon_armor",
        "position": {"x": 3, "y": 0},
        "rotation": 0,
        "comment": "보스 전용 갑옷 (필수)"
      }
    ],
    "autoGeneration": {
      "difficulty": "boss",
      "level": 10,
      "itemBudget": 600,
      "excludeTypes": ["consumable"]
    }
  }
}
```

### 동작 방식
1. `items` 배열의 아이템을 먼저 배치 (Manual)
2. 남은 그리드 공간에 `autoGeneration` 설정으로 아이템 자동 생성 (Auto)
3. 예산은 자동 생성 부분에만 적용

### 장점
✅ 핵심 아이템 보장  
✅ 추가 다양성 제공  
✅ 밸런스 + 리플레이성

### 단점
❌ 설정이 가장 복잡

---

## 🎲 enemyType별 아이템 풀

자동 생성 시 `enemyType`에 따라 다른 아이템 풀에서 선택합니다.

| enemyType | 추가 아이템 풀 |
|-----------|----------------|
| `bandit`  | dagger, bow, poison_vial |
| `monster` | claw, fang, hide |
| `undead`  | cursed_blade, bone_armor, soul_gem |
| `mage`    | staff, spell_book, wand, robe |

---

## 📊 난이도별 예상 결과

| 난이도  | 예산   | 예상 아이템 수 | 예시                                  |
|---------|--------|----------------|---------------------------------------|
| Easy    | 50~100 | 1~2개          | 낡은 검 1개                           |
| Normal  | 100~200| 2~4개          | 철검 1개, 가죽 갑옷 1개               |
| Hard    | 200~400| 4~7개          | 강철검, 사슬 갑옷, 체력 포션 2개      |
| Boss    | 400~800| 6~12개         | 전설 무기, 마법 갑옷, 시너지 아이템들 |

---

## 💡 권장 사용 전략

| 상황 | 권장 모드 | 이유 |
|------|-----------|------|
| 튜토리얼 전투 | Manual | 정확한 난이도 제어 필요 |
| 스토리 보스전 | Manual 또는 Hybrid | 테마 중요, 핵심 아이템 보장 |
| 랜덤 필드 몬스터 | Auto | 많은 수량, 다양성 필요 |
| 미니 보스 | Hybrid | 핵심 장비 + 랜덤성 |
| 일반 적 | Auto | 빠른 설정 |

---

## 📝 실전 예시

### 예시 1: 튜토리얼 전투 (Manual)
```json
{
  "enemyInventory": {
    "mode": "manual",
    "items": [
      {"id": "rusty_dagger", "position": {"x": 0, "y": 0}, "rotation": 0},
      {"id": "torn_cloth", "position": {"x": 2, "y": 0}, "rotation": 0}
    ]
  }
}
```

### 예시 2: 필드 몬스터 (Auto)
```json
{
  "enemyInventory": {
    "mode": "auto",
    "autoGeneration": {
      "difficulty": "normal",
      "level": 3,
      "enemyType": "monster"
    }
  }
}
```

### 예시 3: 보스전 (Hybrid)
```json
{
  "enemyInventory": {
    "mode": "hybrid",
    "items": [
      {"id": "boss_unique_weapon", "position": {"x": 0, "y": 0}}
    ],
    "autoGeneration": {
      "difficulty": "boss",
      "level": 10,
      "itemBudget": 800,
      "excludeTypes": ["consumable"]
    }
  }
}
```

---

## ✅ 체크리스트

**Manual 모드를 사용할 때:**
- [ ] 모든 아이템의 position이 정의되어 있는가?
- [ ] 아이템들이 그리드를 벗어나지 않는가?
- [ ] 아이템들이 서로 겹치지 않는가?

**Auto 모드를 사용할 때:**
- [ ] difficulty와 level이 적절한가?
- [ ] enemyType이 적의 캐릭터와 맞는가?
- [ ] guaranteed 아이템이 필요한가?

**Hybrid 모드를 사용할 때:**
- [ ] 필수 아이템과 자동 생성 부분의 밸런스가 적절한가?
- [ ] autoGeneration 예산이 남은 공간에 적절한가?

---

**Happy Balancing! 🎮**

