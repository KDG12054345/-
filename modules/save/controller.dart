import 'package:flutter/foundation.dart';

class SaveController with ChangeNotifier {
  // P0: 기존 SaveSystem 어댑터는 P1에서 연결
  
  @override
  void dispose(){ 
    super.dispose(); 
  }
}





























